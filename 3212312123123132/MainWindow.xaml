<Window x:Class="WpfApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainWindow" Height="427" Width="930">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="Имя:" Grid.Row="0" Grid.Column="0" Margin="5"/>
        <TextBox x:Name="TbxName" Grid.Row="0" Grid.Column="1" Margin="5"/>

        <TextBlock Text="Возраст:" Grid.Row="1" Grid.Column="0" Margin="5"/>
        <TextBox x:Name="TbxAge" Grid.Row="1" Grid.Column="1" Margin="5"/>

        <Button x:Name="BtnSerialize" Content="Сериализовать" Grid.Row="2" Grid.Column="0" Margin="5" Click="BtnSerialize_Click"/>
        <Button x:Name="BtnDeserialize" Content="Десериализовать" Grid.Row="2" Grid.Column="1" Margin="5" Click="BtnDeserialize_Click"/>
    </Grid>
</Window>
